---
const { href, ...imgProps } = Astro.props;
---

<figure>
  {
    href ? (
      <a href={href}>
        <img {...imgProps} />
      </a>
    ) : (
      <img {...imgProps} />
    )
  }
  <figcaption>{imgProps.alt}</figcaption>
</figure>
